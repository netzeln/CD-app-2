
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/styles.css" type="text.css">
        <title>Your CD Collection</title>
    </head>
    <body>
        <h1>CD Collection</h1>
        <h3><a href="/add_CD">Add some CDS to your collection</a></h3>
        <h3><a href="/delete">Delete and get an iPod</a></h3>

        <form  action="/search" method="post">
            
            <input type="text" name="searchCDs" id="searchCDs">
            <button type="submit">Search for an Artist</button>

        </form>

        {% if cds is not empty %}
            {% for cd in cds %}
                <div class='row'>
                    <div class='col-md-6'>
                        <img src="{{cd.getCoverArt}} ">
                    </div>
                    <div class='col-md-6'>
                        <p> {{cd.getTitle}} </p>
                        <p>{{cd.getArtist}} </p>
                    </div>
                </div>
            {% endfor %}
        {% endif %}

        {% if cds is empty %}
            <h2> You must have an iPod</h2>
            <h3><a href="/add_CD">Add some CDS to your collection</a></h3>
        {% endif %}
    </body>
</html>
