
<html>
    <head>
        <meta charset="utf-8">
        <title>Your CD Collection</title>
    </head>
    <body>
        <h1>hello</h1>
        {% if cds is not empty %}
            {% for cd in cds %}
                <div class='row'>
                    <div class='col-md-6'>
                        <img src="{{cd.getCoverArt}} ">
                    </div>
                    <div class='col-md-6'>
                        <p> {{cd.getTitle}} </p>
                        <p>{{cd.getArtist}} </p>
                    </div>
                </div>
            {% endfor %}
        {% endif %}

        {% if cds is empty %}
            <h2> You must have an iPod</h2>
            <h3><a href="/add_cd">Add some CDS to your collection</a></h3>
        {% endif %}
    </body>
</html>
