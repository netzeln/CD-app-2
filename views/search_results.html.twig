<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        {% if results is not empty %}
            {% for result in results %}
                <div class='row'>
                    <div class='col-md-6'>
                        <img src="{{result.getCoverArt}} ">
                    </div>
                    <div class='col-md-6'>
                        <p> {{result.getTitle}} </p>
                        <p>{{result.getArtist}} </p>
                    </div>
                </div>
            {% endfor %}
        {% endif %}

        {% if cds is empty %}
            <h2> No cds match your search term</h2>
            <h3><a href="/">Return Home</a></h3>
        {% endif %}
    </body>
</html>
